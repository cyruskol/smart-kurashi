---
title: "Gemini 3.5 Flash が Computer Use を内蔵——AI エージェントはなぜ次に「画面の操作」を始めたのか"
excerpt: "Google が Gemini 3.5 Flash に Computer Use を標準機能として統合することで、AI エージェントはブラウザ・モバイル・デスクトップを自在に横断できるようになりました。その背景にある戦略と、企業・個人の仕事方への影響を解説します。"
date: "2026-06-25"
category: "ai-tech"
tags: ["Google", "Gemini", "Computer Use", "AI エージェント", "自動化", "Agent", "Vertex AI"]
image: "/images/blog/gemini-35-flash-computer-use-hero.png"
source: "Smart Kurashi × Google Blog"
---

![Gemini 3.5 Flash Computer Use のコンセプトイメージ](/images/blog/gemini-35-flash-computer-use-hero.png)

# Gemini 3.5 Flash が Computer Use を内蔵——なぜ「画面操作」が AI の次の主戦場になったのか

## はじめに：AI は「アドバイス」から「実行」へ

2026年6月24日、Google は Gemini 3.5 Flash に「Computer Use」を標準機能として統合すると発表した。これは、これまで単体モデルとして提供されていた「Gemini 2.5 Computer Use」を、メインフラッシュモデルの方に内包化するという意味だ。

この変更は、AI が「何かを教えてくれるもの」から「実際に画面の上で動いてくれるもの」へと移行する大きな転換点となる。開発者は今後、ブラウザ上のフォーム入力、モバイルアプリの操作、デスクトップアプリケーションの処理——これらを一つの AI エージェントにまとめて任せることができるようになるのだ。

[OpenAI が初のカスタムチップ「Jalapeño」を発表](/posts/2026-06-24_openai-jalapeno-custom-chip-broadcom-ai-inference) したばかりの今、AI インフラの競争はコスト削減だけでなく「AI がどう動くか」そのものの変化も加速させている。

---

## Computer Use とは——AI に「目」と「手」を与える技術

### 従来の AI エージェントの限界

これまでの AI エージェントは、API を通じて特定のツールを呼び出す仕組みが主だった。開発者が事前に定義した範囲でしか動けない。

しかし現実の仕事は、API でラップされていない操作の連続だ。Web のフォーム入力、社内システムからデータ抽出、モバイルアプリの設定変更、複数タブの情報収集——これらを一つずつコードで定義するのは非現実的だ。

![Computer Use が操作する画面のイメージ](/images/blog/gemini-computer-use-screen-operation.png)

#### 従来の方法と Computer Use の違い

| 方式 | 操作範囲 | 柔軟性 | 開発コスト |
|------|----------|--------|------------|
| Function Calling / API 連携 | 事前定義された機能のみ | 低 | 高（実装が必要） |
| RAG + コード実行 | データ処理と推論に特化 | 中 | 中 |
| Computer Use（画面操作） | 画面上のあらゆる操作 | 高 | 低（自然言語で指示） |

### Computer Use の基本的な仕組み

Computer Use を搭載した AI エージェントは、以下のステップで動作する。

1. **画面の認識**：スクリーンショットを取得し、現在の状態を理解する。AI は画像認識モデルを使って UI 要素を特定し、テキスト領域（OCR）やボタン・リンクなどのインタラクションポイントを抽出する
2. **意思決定**：AI が「次に何すべきか」を判断する。ユーザーの指示と画面の状態を照合し、最適なアクションを選択する
3. **操作の実行**：クリック、スクロール、キー入力などのアクションを実行する。マウス座標やキーコードを正確に送信することで、人間が実際にマウスとキーボードを使うのとほぼ同じ挙動を実現する
4. **結果の確認**：操作後の画面状態を確認し、次の手順を決定する。AI は実行した操作の結果を検証し、意図しない変化がないかチェックする

このサイクルを繰り返すことで、AI は人間がマウスとキーボードで行うのとほぼ同じ操作を自律的に行えるのだ。ただし、実際の運用では「画面の認識」に数秒から数十秒かかることもあり、複雑なタスクでは全体で数分〜十数分の時間がかかることを理解しておく必要がある。

---

## Gemini 3.5 Flash の Computer Use がもたらすもの

### Flash モデルへの統合という意味

従来の Gemini 2.5 では、Computer Use は専用のモデルとして提供されていた。開発者は通常の推論タスクと Computer Use タスクでモデルを使い分ける必要があり、システム構成は複雑だった。

Gemini 3.5 Flash への統合により、単一のモデルで通常の会話・推論から画面操作までをシームレスに扱える。

### 3つの環境を横断する

Gemini 3.5 Flash の Computer Use は、ブラウザ操作、モバイルアプリ操作、デスクトップ操作の3環境をカバーする。これらは統合的に動作するため、「ブラウザで情報を集め、スプレッドシートにまとめて、メールで送信する」といった一連のワークフローも一つのエージェントで完結できる。

### 安全性へのアプローチ

Computer Use は強力な分、リスクも伴う。AI が勝手に画面を操作することで、意図しないデータ送信が起こりうる。

Google は、敏感的な操作に対するユーザー確認機能や、プロンプトインジェクションを自動検知して停止する機能を提供する。企業は安全なサンドボックス・ヒューマンインザループ・厳密なアクセス制御と組み合わせて運用する。

![Gemini 3.5 Flash セキュリティ機能の概念図](/images/blog/gemini-35-flash-safeguards.png)

---

## 具体的なユースケース——どんなことが自動化できるのか

### フォーム入力やデータ転記

Web のフォームに入力する作業は、多くの人が日常的に行っている。手動で入力するのは時間がかかるだけでなく、誤字脱字のリスクもある。Computer Use を使えば、「このフォームに名前と住所を入力して送信してください」といった自然言語の指示だけで、AI が適切なフィールドを特定し、値を入力・送信できる。

また、複数の Web サイトから情報を収集して Excel や Google スプレッドシートにまとめる作業も自動化可能だ。「Amazon のベストセラーランキング 10 位までをリストアップして CSV に出力してください」と指示すれば、AI は各ページを巡回し、データを抽出・整形してファイルとして保存してくれる。

### Web スクレイピングの進化

従来のスクレイピングは HTML パースや CSS セレクターの知識が必要で、サイト構造が変わるとすぐに壊れる。Computer Use なら「このサイトの製品ページから価格と在庫数を取得してください」と指示するだけで、AI が動的に UI を読み取りながらデータを収集できる。JavaScript でレンダリングされた要素も問題なく扱えるため、現代の Web サイトとの親和性が高い。

### マルチステップワークフロー

複数のタスクを連続して実行するワークフローも可能になる。「旅行サイトの比較ページを開いて、3 つの航空会社の往復便料金をメモし、それをメールで送ってください」といった指示は、ブラウザ操作からデータ抽出、メール作成までを一連の流れとして処理できる。これにより、開発者は API を一つずつ定義する必要がなくなり、自然言語だけで複雑なタスクを指定できるようになる。

### モバイルアプリの操作

Computer Use はモバイルアプリにも対応する。「このアプリで新しいアカウントを作成して、プロフィール画像を設定してください」といった指示は、スマホの画面を認識し、タップやキーボード入力をシミュレートすることで実現できる。ただし、モバイル環境ではタッチイベントの精度や、通知バナーなどの UI 要素が操作を邪魔することがあるので、適切なハンドリングが必要だ。

### デスクトップアプリケーションとの連携

デスクトップアプリ（例：スプレッドシートソフト、メールクライアント）も対象となる。「この Excel ファイルを開いて、A1 セルに『売上データ』と入力してください」といった指示は、OS 上のアプリケーションを認識し、キーボード入力をシミュレートすることで実行できる。ただし、セキュリティ上、ファイルの読み書きやネットワーク通信には制限がかかる場合があるため、サンドボックス環境での実行が推奨される。

---

---

## なぜ今 Computer Use なのか——Google の戦略

### AI エージェント市場の激化

2026 年、AI エージェント市場は巨大な戦略的投資を集めている。Anthropic は Claude のツール利用能力を強化し、OpenAI はエージェント製品「Codex」を展開中だ。AI が「ただのチャットボット」から「実際に動くエージェント」へと進化する中で、画面操作能力は必須の差別化要因となっている。

Google にとって Gemini 3.5 Flash への Computer Use 統合は、Vertex AI / Google Cloud との親和性、Google Workspace との統合、そして検索・インデックス技術の優位性という 3 つの戦略的意味を持つ。

### 市場における位置づけ

Computer Use の登場は、AI エージェントが「画面」という新しいインターフェースを獲得したことを意味する。従来の API 連携では対応できなかった領域——Web サイトの動的コンテンツ、モバイルアプリの UI、デスクトップアプリケーション——これらを AI が直接操作できるようになったのは画期的だ。

特に注目すべきは、Google の検索技術との親和性である。Computer Use は画面を認識・理解するため、検索で得た情報と画面の状態を統合してタスクを実行できる。例えば「最新のニュース記事の要約をまとめてください」といった指示では、AI が検索結果を読み込み、各記事の画面から情報を抽出し、まとまったレポートを作成するといったことが可能になる。

### 技術的な優位性

Gemini 3.5 Flash は、従来の Large Language Model に加えて、画像認識や OCR（光学文字認識）の能力も内蔵している。このため、Computer Use ではスクリーンショットからテキストを抽出し、UI 要素を特定する処理がスムーズに行える。また、Flash モデルへの統合により、推論と画面操作を一つのモデルでシームレスに扱えるようになり、システム構成が大幅に簡素化された。

### 競合との比較

OpenAI の Computer Use は主にブラウザ操作に特化しており、モバイルやデスクトップアプリには対応していない。Claude も同様に Web ブラウザでのタスク実行を重視している。Google の Computer Use はこれらと比べて、3 つの環境（ブラウザ・モバイル・デスクトップ）を統合的にカバーする点で優位性があると言える。

### 開発者体験への影響

Computer Use を搭載した AI エージェントは、自然言語だけで複雑なタスクを指定できるため、開発者の負担が大幅に軽減される。従来の API 連携では、各サービスごとにエンドポイントや認証方式を理解し、コードで定義する必要があったが、Computer Use では「このフォームに入力して送信してください」といった指示だけで済む。ただし、AI の判断が意図と異なる場合の検証・制御は開発者の責任となるため、「何をさせるか」「どう止めるか」「どう検証するか」という設計思考が重要になる。

---

### 前回の記事との関連性

先日、[AI エージェント PC の時代](/posts/2026-06-20_ai-agent-pc-era-nvidia-rtx-spark-amd-gaia) についての記事で、AI エージェントが PC 上で自律的に動く未来を紹介した。Computer Use はまさにその「PC 上で動く」部分を実現する技術であり、この流れをさらに加速させるものだ。

---

## 企業への影響——何が変わるのか

### ソフトウェアテストの自動化

Computer Use がもたらす直接的なインパクトの一つがソフトウェアテストだ。従来はテストケースをコードで記述し、画面変更に合わせてメンテナンスする必要があった。しかし Computer Use を搭載した AI エージェントなら、自然言語で指示を与えるだけでテストが実行される。画面が変わっても AI が適応するため、保守コストは大幅に下がる。

### ナレッジワークの効率化

企業のナレッジワーク——調査、データ収集、レポート作成、システム入力——は多くの時間を占める。Computer Use によって、複数サイトの情報収集・集計、データ転記、メールに基づく予定作成などの作業が自動化できる。

### 開発者の役割の変化

Computer Use の普及は開発者の仕事も変える。これまで開発者は「API を書く」「UI テストを書く」「ワークフローをコードで定義する」といった作業が主だった。しかし AI エージェントが画面を直接操作できるようになれば、「何をさせるか」「どう止めるか」「どう検証するか」といった上位の設計に集中できる。

---

## 個人ユーザーへの影響

### ブラウザ操作の自動化

個人ユーザーにとっても、Computer Use は日常的な作業の効率化につながる。旅行サイトの比較、EC サイトでの商品検索・価格比較、SNS 通知の整理、オンライン申請手続きの完了——これらはすべて「画面の操作」を伴うため、API では対応できなかった領域だ。

### アクセシビリティの向上

Computer Use は、視覚障害や身体障害を持つ人々のアクセシビリティ向上にも貢献する。従来は画面の操作にマウスやタッチが必要だった多くのタスクを、音声やテキストの指示だけで完結できるようになるからだ。

![Computer Use によるアクセシビリティ向上のイメージ](/images/blog/computer-use-accessibility.png)

---

## 今後の展望——Computer Use はどこへ向かうのか

### マルチモーダルな操作能力の進化

Computer Use はまだ初期段階だ。今後は、音声と画面操作を組み合わせたエージェント体験、デバイスを跨ぐ操作の実現、ユーザーパターンを学習した個人化自動化の提案といった進化が予想される。

### 標準化の動き

Computer Use のような画面操作型エージェントは、まだ各社バラバラの実装で提供されている。将来的には標準化団体を通じて標準規格が策定される可能性もある。これにより、一つのエージェントが複数のサービスで動けるようになる。

---

## まとめ——「動く AI」が日常になる日

Gemini 3.5 Flash への Computer Use 統合は、AI が「答えを出すもの」から「実際に動くもの」へと進化する一つのマイルストーンだ。

OpenAI がインフラコストの削減に注力する一方で、Google は AI エージェントの「行動力」を高めることで差別化を図っている。AI の未来はこうした多様な競争によって形作られていく。

私たちが普段使っている Web サービスやアプリが、そのまま AI エージェントの操作対象になる——そんな時代が近づいている。あなたは明日、どんな仕事を AI に任せてみたいだろうか。

---

*関連記事：[OpenAI が初カスタムチップ「Jalapeño」を発表——Broadcom 共同開発で AI 推論の効率を革新する](/posts/2026-06-24_openai-jalapeno-custom-chip-broadcom-ai-inference) | [AI エージェント PC の時代——NVIDIA RTX Spark と AMD Gaia が描く近未来](/posts/2026-06-20_ai-agent-pc-era-nvidia-rtx-spark-amd-gaia)*

---

## Computer Use の技術的仕組み——AI は画面をどう「見て」「動かす」のか

### 視覚的な理解プロセス

Gemini 3.5 Flash の Computer Use は、スクリーンショットを入力として受け取り、画面上の要素を「視覚的に」理解する。これは単なるOCR（文字認識）ではなく、ボタン、テキストフィールド、メニュー、アイコンなどのUI要素を構造的に認識する仕組みだ。

具体的には、以下のプロセスで動作する。

1. **スクリーンショット取得**：現在の画面キャプチャを取得
2. **要素認識**：画面上のクリック可能な領域、入力フィールド、テキストを特定
3. **座標計算**：対象要素の画面上の座標を計算
4. **操作実行**：マウスクリック、キーボード入力、スクロールなどを実行
5. **結果確認**：操作後の画面変化を確認し、次のアクションを判断

この一連の流れは、人間が画面を見て操作するプロセスと基本的に同じだ。ただし、AI は一度に画面全体を「見」て、最適な操作パスを計算できる点で人間より優れている場合がある。

### 従来の API 呼び出しとの違い

従来の AI エージェントは、開発者が事前に定義した API（関数呼び出し）を通じて外部サービスと連携していた。例えば「天気予報 API に聞く」「データベースから検索する」といった具合だ。

しかし Computer Use では、API が存在しない操作でも可能になる。例えば、まだ API を提供していない Web サービスのフォーム入力、独自の業務アプリの操作、あるいは人間しか操作できないような画面——これらを AI が直接操作できるようになる。

---

## 具体的なユースケース——何ができるようになるのか

### フォーム入力の自動化

最も基本的なユースケースは、Web フォームの自動入力だ。住所変更、予約申し込み、資料請求など、繰り返し行われる入力作業を AI が自動で完了してくれる。

特に日本では、政府のオンライン申請（e-Gov など）や銀行のオンラインバンキングなど、複雑なフォームを扱う場面が多い。Computer Use が普及すれば、これらの操作を AI に任せられる可能性がある。

### マルチステップワークフローの実行

より複雑なユースケースとして、複数のアプリケーションを跨ぐワークフローの自動化がある。例えば：

- メールで届いた注文確認を開く
- 社内の在庫管理システムにアクセス
- 在庫数を確認して発注処理
- 発注確認メールを送信

こうした一連の作業を、AI エージェントが自律的に実行できるようになる。

### データ収集・スクレイピング

Computer Use を使えば、API を公開していない Web サイトからもデータを収集できる。AI が画面を「見て」必要な情報を抽出し、構造化データとして整理する。

ただし、利用規制でスクレイピングが禁止されているサイトでは、この機能を使う際には注意が必要だ。

---

## 日本市場への影響と展望

### 日本企業が Computer Use を活用するメリット

日本は労働力不足が深刻な課題となっている。特に中小企業では、バックオフィス業務に割くリソースが限られている。Computer Use が普及すれば、限られた人的リソースをより戦略的な業務に集中させることが可能になる。

また、日本の Web サービスは海外に比べて UI が複雑なケースが多い。Computer Use のような「画面を理解する」技術は、こうした複雑な UI を効率的に操作する強力なツールとなる。

### 今後の課題

Computer Use にはいくつか解決すべき課題がある。

- **プライバシー**：AI が画面を「見る」ため、機密情報が漏洩するリスク
- **誤操作**：AI が誤った操作をした場合の責任の所在
- **セキュリティ**：悪意のある AI エージェントによる不正操作

これらの課題に対しては、技術的な対策（サンドボックス実行、操作ログの記録など）と法的な枠組みの整備が並行して進む必要がある。

---

## まとめ——「動く AI」が日常になる日

Gemini 3.5 Flash への Computer Use 統合は、AI が「答えを出すもの」から「実際に動くもの」へと進化する一つのマイルストーンだ。

OpenAI がインフラコストの削減に注力する一方で、Google は AI エージェントの「行動力」を高めることで差別化を図っている。AI の未来はこうした多様な競争によって形作られていく。

私たちが普段使っている Web サービスやアプリが、そのまま AI エージェントの操作対象になる——そんな時代が近づいている。あなたは明日、どんな仕事を AI に任せてみたいだろうか。

---

*関連記事：[OpenAI が初カスタムチップ「Jalapeño」を発表——Broadcom 共同開発で AI 推論の効率を革新する](/posts/2026-06-24_openai-jalapeno-custom-chip-broadcom-ai-inference) | [AI エージェント PC の時代——NVIDIA RTX Spark と AMD Gaia が描く近未来](/posts/2026-06-20_ai-agent-pc-era-nvidia-rtx-spark-amd-gaia)*
